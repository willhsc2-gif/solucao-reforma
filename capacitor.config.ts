import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.soluoreforma',
  appName: 'Solução Reforma',
  webDir: 'dist'
};

export default config;
